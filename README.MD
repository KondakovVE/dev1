Это учебный проект, в котором на примерах я разбираюсь с работой Docker, FastAPI, Postgree

Для запуска надо:

- поднять докер `docker compose up`
- запустить сервис `poetry run uvicorn app.main:app --debug`
